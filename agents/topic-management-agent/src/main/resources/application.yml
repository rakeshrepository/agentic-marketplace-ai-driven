# Topic Management Agent Configuration

server:
  port: 8080

spring:
  application:
    name: topic-management-agent

# LLM Configuration (config-driven)
llm:
  provider: ${LLM_PROVIDER:ollama}  # Options: ollama, openai
  timeout: ${LLM_TIMEOUT:60000}

# Ollama Configuration (when provider=ollama)
ollama:
  base-url: ${OLLAMA_BASE_URL:http://localhost:11434}
  model: ${OLLAMA_MODEL:llama3.2}

# OpenAI Configuration (when provider=openai)
openai:
  api-key: ${OPENAI_API_KEY:}
  model: ${OPENAI_MODEL:gpt-4o-mini}
  base-url: ${OPENAI_BASE_URL:https://api.openai.com/v1}

# MCP Server Configuration
mcp:
  kafka:
    base-url: ${MCP_KAFKA_URL:http://localhost:8081}
    timeout: ${MCP_TIMEOUT:30000}
  email:
    url: ${MCP_EMAIL_URL:http://localhost:8084}
    timeout: ${MCP_EMAIL_TIMEOUT:30000}

# SSL Configuration
# WARNING: Set ssl.trust-all=true ONLY in development environments
# In production, use proper SSL certificates and keep this as false
ssl:
  trust-all: ${SSL_TRUST_ALL:false}

# Agent Configuration
agent:
  id: topic-management-agent
  name: Kafka Topic Management Agent
  category: infrastructure

# Logging
logging:
  level:
    com.agentic.marketplace: DEBUG
    org.springframework.web: INFO
